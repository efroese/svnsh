# Created by repocreate.py
# DO NOT EDIT THIS FILE BY HAND
# USE svnsh

<Location /svn/%(repopath)s>

  DAV svn
  SVNPath /repos/%(repopath)s

  AuthType Basic
  AuthName "%(repopath)s subversion repository"

  LDAP_CertDbDir /etc/http.d/certs
  LDAP_Server ldap.example.com
  LDAP_Port 636

  AuthOnBind on
  Base_DN "ou=People,o=com"
  UID_Attr uid

  Require valid-user

  #SVN Access controls
  AuthzSVNAccessFile %(apache_authz_path)s

  #Allow fall through for non-LDAP accounts
  AuthLDAPAuthoritative Off
  AuthAuthoritative Off
  AuthUserFile /etc/httpd/htpasswd
</Location>
